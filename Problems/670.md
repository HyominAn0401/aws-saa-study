### 📝 670. 서버리스 SFTP 마이그레이션 
가용성 높은 SFTP 서비스를 탄력적 IP로 실행되는 두 개의 EC2 인스턴스를 사용해 인터넷에서 신뢰할 수 있는 IP 소스의 트래픽을 허용. 공유 스토리지를 통해 서비스가 지원되며 사용자 계정은 Linux 사용자로 생성 및 관리됨
높은 IOPS 성능과 고도로 구성 가능한 서버리스 옵션을 원하고 사용자 권한에 대한 제어를 유지하고자 함

---

### 🎯 1. 요구사항 분석
* **서버리스 옵션**: EC2를 직접 관리하지 않겠다 -> AWS Transfer Family 
* **공유 스토리지**: 여러 SFTP 사용자가 동일한 파일 조회 가능 -> S3 or EFS 
* **높은 IOPS 성능**: 파일 읽기/쓰기가 매우 빨라야 함 -> 일반적인 S3 보다는 파일 시스템 계층인 EFS 
* **사용자 권한 제어 유지**: 기존 Linux 사용자 계정처럼 세세한 권한 제어 유지 

---

### 🔍 2. 핵심 서비스 분석

| 서비스/용어명 | 핵심 특징 | 비고 |
| :--- | :--- | :--- |
| **SFTP** | FTP에 보안을 추가해 파일을 안전하게 주고받는 프로토콜<br>데이터를 대량으로 전송하거나 오래된 시스템에서 파일을 업로드할 때 가장 많이 쓰이는 표준 방식 |  |
| **IOPS(Input/Output Operations Per Seconds)** | 저장 장치가 1초에 얼마나 많은 읽기/쓰기 작업을 할 수 있는가를 나타내는 성능 단위 | IOPS가 높을수록 복잡하고 무거운 작업을 빠르게 처리 |
| **Elastic Block Store(EBS)** | EC2에 사용하는 가상 외장 하드디스크<br>데이터를 블록 단위로 쪼개서 관리해 매우 빠름<br>하나의 EC2에 하나의 EBS | | 
| **AWS Transfer Family (for SFTP)** | SFTP 서버를 직접 구축하지 않고 AWS가 대신 관리하는 서비스 | 업로드된 파일은 자동으로 S3나 EFS에 저장됨 |
| **Elastic File System(EFS)** | 여러 대의 EC2가 동시에 연결해서 사용할 수 있는 공용 네트워크 공유 폴더 | 용량이 늘어나면 자동으로 크기가 커지는 리눅스 전용 파일 시스템 |
| **Elastic IP** | 고정 공인 IP 주소 | 서버 재시작해도 IP 불변 |

---

### ✅ 3. 선택지 분석 및 풀이

- [ ] **A**: EBS 볼륨 사용 
    - **이유**: EBS는 단일 인스턴스 전용 블록 스토리지. SFTP의 공유 스토리지로 적합하지 않음 
- [X] **B**: EFS 볼륨 + VPC 엔드포인트 + 보안그룹 
    - **결과**: **정답**
    - **이유**: 
        1. 사용자 권한 제어: EFS는 Linux 표준 권하능ㄹ 지원
        2. 높은 IOPS: EFS는 프로비저닝된 처리량 모드를 통해 높은 IOPS와 대역폭을 명시적 설정 가능 
        3. 정교한 보안: VPC 엔드포인트를 사용하면 SFTP 서비스 입구를 VPC 안에 둘 수 있고 보안그룹을 붙여 신뢰할 수 있는 IP만 허용하도록 세밀하게 제어 가능 
- [ ] **C**: S3 버킷 + 퍼블릭 엔드포인트
    - **이유**: S3는 객체 스토리지로 Linux의 파일 권한을 그대로 구현하기 어려움<br>또한 퍼블릭 엔드포인트는 인터넷에 노출되어 있어 VPC 내부에서 보안 그룹으로 제어하는 것보다 정교함이 떨어짐 
- [ ] **D**: S3 버킷 + 내부 전용 VPC 엔드포인트 
    - **이유**: 내부 액세스 권한만 있는 엔드포인트를 사용하면 인터넷을 통해 접속하는 외부 사용자가 서비스를 사용할 수 없음 => 완전히 내부 전용으로 만들면 접속 자체가 안 됨 

---

### 💡 핵심 요약
1. Linux 사용자 권한 제어 + 높은 IOPS -> AWS Transfer Family 파트너로 EFS (리눅스 권한 유지 = EFS)
2. 보안을 위해 VPC 엔드포인트 + 보안그룹 -> 고도로 구성 가능한 보안 의 표준 모델 